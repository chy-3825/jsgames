const CamelUpEngine = require('./engine');
const metadata = { type: 'camelup', name: '狂野骆驼', minPlayers: 3, maxPlayers: 8 };
class CamelUpSession { constructor(roomId, players, options = {}) { this.options = options && typeof options === 'object' ? { ...options } : {}; this.engine = new CamelUpEngine(roomId, players, this.options); this.started = false; } start() { const result = this.engine.start(); if (result.success) this.started = true; return result; } handleAction(playerId, action) { return this.started ? this.engine.handleAction(playerId, action) : { success: false, message: '游戏尚未开始' }; } handlePlayerLeave(playerId) { return this.engine.handlePlayerLeave(playerId); } getPlayerState(playerId) { return this.engine.getPlayerState(playerId); } getPlayerAction(action) { return action; } getWinner() { return this.engine.getWinner(); } }
module.exports = { metadata, create(roomId, players, _hostId, options = {}) { return new CamelUpSession(roomId, players, options); } };
