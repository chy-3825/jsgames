# 2026-09-10 朋友内测准备

本记录针对当前工作区，不代表所有历史提交或目标云服务器已经验收。

## 2026-09-15 当前工作树复测

本节覆盖本轮修复后的可重复本机门禁；旧章节保留为历史内测记录，不与本次数字混用。

- `npm run test:acceptance`：通过。依赖审计 0 vulnerabilities；语法 423 个文件；lint、类型、展示层事件审计、复杂度、报告、发布元数据、生产部署和 `git diff --check` 均通过。
- `npm test`：733/733 通过，0 失败；覆盖率为语句/行 87.65%、分支 77.43%、函数 81.21%。
- Firefox：28/28 模块与 96/96 视觉夹具通过；本轮修复了短横屏 Manila 棋盘裁切/溢出，并统一入口脚本资源版本。
- 性能与清理：40/40 HTTP 请求、20 个临时大厅连接、6 个房间/12 名玩家回收通过；静态资源预算通过。
- 预览包：`dist/jsgames-2.1.0-preview-4cb35574c274.tar.gz`，634 个运行文件，SHA-256 `436df18453c7e1adb7a37d4fc6ddd4e85f11c4710ce72bb6f4f438d74d5ea62a`；独立目录执行 `npm ci --omit=dev --ignore-scripts`（0 vulnerabilities）并以生产模式启动 `/healthz` 返回 200。
- 当前运行环境为本机 Node `v24.15.0`；CI 仍以 Node 20.x/22.x 为支持矩阵，本节不能代替 CI 实际运行。
- 本轮未创建新提交或发布标签，未执行 Chromium、Safari、真实 iOS/Android、目标服务器、真实网络切换和素材许可证签字。

## 修复

- 大富翁纸牌缩窄手机牌库栏，防止挤出选中牌；桌面卡牌分隔留白统一为 12px；短横屏操作区使用可收缩网格，避免按钮区域裁切。浏览器烟测增加行动操作区边界检查。
- 地产牌测试核对实际校正后的 PNG；移动端测试核对手牌与操作并列的新布局。
- 旧封面及缩略图迁移到 `review/archived-covers/`，保留原稿；线上目录只保留当前 28 款封面。
- 生产模式屏蔽评审页面、视觉夹具和开发说明；性能预算统计相同的生产资源集合，3.6 MiB 上限不变。
- 生产默认监听 `127.0.0.1`，可用 `JSGAMES_HOST` 覆盖；systemd 模板显式绑定回环地址。
- 通过 overrides 固定 `qs@6.16.0`，保留 Express 4。修复依据：[上游公告](https://github.com/advisories/GHSA-x5fp-wj9c-mxmx)、[上游拒绝服务公告](https://github.com/advisories/GHSA-4mjr-xmp4-gh2g)。
- 新增 `npm run package:preview`：按运行目录收集当前工作区，包含未跟踪模块，生成包内 SHA-256 文件清单与压缩包校验文件。不提交、不发布。
- 报告门禁显式登记补充报告，仍严格核对 28 份游戏报告。

## 已执行

- 自动化回归：722/722。
- 覆盖率：行/语句 87.92%，分支 77.31%，函数 81.83%。
- Firefox：28 个模块导入、大厅生命周期、短时传输断线恢复、隐私输入与 96/96 视口通过。清理重复 CSS 后另复测纸牌 4/4 视口并查看手机截图。
- 本地性能烟测：40 个 HTTP 请求、20 个大厅连接、6 个房间清理通过；生产首方源码约 3.44 MiB，低于 3.6 MiB 预算。
- 部署烟测：生产进程启动、健康检查、安全头、开发路径 404、首页与游戏模块 200、WebSocket 建房与 SIGTERM 通过。
- ESLint、渐进类型检查、语法检查、复杂度门禁、报告门禁、发布门禁、`git diff --check` 通过；生产依赖审计 0 漏洞。
- 内测包：`dist/jsgames-2.1.0-preview-1e4c8aa005f4.tar.gz`，634 个运行文件；逐文件哈希核对通过。在独立临时目录解压、`npm ci --omit=dev --ignore-scripts` 干净安装及生产部署烟测通过。
- 当前未发现可用 Chromium 可执行文件，本轮未运行 Chromium 烟测。

## 边界

房间仍保存在单进程内存中，服务重启会丢失对局；本轮未实现持久化或多实例。应安排停局维护。

尚未操作云服务器、域名、证书或安全组，真实手机外网整局和实际网络切换仍需部署后执行。包是固定的工作区快照，Git 提交与标签尚未创建。
